<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    {% load static %}
    <link rel = "stylesheet" type = "text/css" href = "{% static 'css/style.css' %}" />
    
</head>
<body>
    <h2 class = "classTitle">Voici un contenu affiché depuis le server</h2>
    <img src = "{% static 'images/img.png' %}">
    {% for element in materielsinfo %}
        {{ element }} <br>
    {% endfor %}
    <hr>
    {% if age  > 18 %}
        majeur
    {% else %}
        mineur
    {% endif %}
    
    

</body>
</html>